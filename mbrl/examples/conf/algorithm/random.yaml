#### @package _group_
name: "random"
environment_steps: 10000
dataset_size: 10000

agent: mbrl.planning.RandomAgent

model_trainer: ${dynamics_model.model_trainer}
reinitialize_models: ${dynamics_model.reinitialize_models} 
normalize: ${dynamics_model.normalize}
normalize_double_precision: ${dynamics_model.normalize_double_precision}
target_is_delta: ${dynamics_model.target_is_delta}

initial_exploration_steps: ${overrides.trial_length}
freq_train_model: ${overrides.freq_train_model}
learned_rewards: ${overrides.learned_rewards}

num_particles: 20
